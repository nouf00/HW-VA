import { hasTopLevelTransformingEffect } from "./common-helpers.js";
import { IOSchemaError } from "./errors.js";
export const createMiddleware = (props) => {
    if (hasTopLevelTransformingEffect(props.input)) {
        throw new IOSchemaError("Using transformations on the top level of middleware input schema is not allowed.");
    }
    return {
        ...props,
        type: "proprietary",
    };
};
//# sourceMappingURL=middleware.js.map