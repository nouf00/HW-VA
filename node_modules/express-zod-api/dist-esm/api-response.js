import { mimeJson } from "./mime.js";
export const createApiResponse = (schema, mimeTypes = mimeJson) => {
    return {
        schema,
        mimeTypes: typeof mimeTypes === "string" ? [mimeTypes] : mimeTypes,
    };
};
//# sourceMappingURL=api-response.js.map